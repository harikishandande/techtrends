# Input values for the PRODUCTION environment.
# Values not specified here are inherited from values.yaml.

namespace:
  name: prod

image:
  pullPolicy: Always

replicaCount: 5

service:
  port: 7111

resources:
  requests:
    memory: 128Mi
    cpu: 350m
  limits:
    memory: 256Mi
    cpu: 500m
